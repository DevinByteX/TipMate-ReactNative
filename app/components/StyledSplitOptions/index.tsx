import React from 'react';
import { View, Text } from 'react-native';

type StyledSpiltOptionsProps = {};

export const StyledSpiltOptions = (props: StyledSpiltOptionsProps) => {
  return (
    <View>
      <Text>StyledSpiltOptions</Text>
    </View>
  );
};
