import React from 'react';
import {Text, View} from 'react-native';

type styledHeaderProps = {};

export const StyledHeader = ({}: styledHeaderProps) => {
  return (
    <View>
      <Text>StyledHeader</Text>
    </View>
  );
};
